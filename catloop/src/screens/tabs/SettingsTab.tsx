import React from "react";
import { Linking, Pressable, ScrollView, StyleSheet, Text, View } from "react-native";
import { Badge, Caption } from "../../components/ui";
import { confirmAction, notify } from "../../lib/dialog";
import { LINKS } from "../../lib/links";
import { Entitlement, isSubscribed, useStore } from "../../state/store";
import { colors, cardShadow, font, radius, spacing } from "../../theme";

function planLabel(e: Entitlement): string {
  if (e === "trial") return "Starter — 3-day trial";
  if (e === "weekly") return "Starter — $4.95 / week";
  return "No active plan";
}

function Row({
  label,
  value,
  onPress,
  danger,
}: {
  label: string;
  value?: string;
  onPress?: () => void;
  danger?: boolean;
}) {
  return (
    <Pressable
      onPress={onPress}
      disabled={!onPress}
      style={({ pressed }) => [styles.row, { opacity: pressed && onPress ? 0.7 : 1 }]}
    >
      <Text style={[styles.rowLabel, danger && { color: colors.ember }]}>{label}</Text>
      {value ? <Text style={styles.rowValue}>{value}</Text> : onPress ? <Text style={styles.chevron}>›</Text> : null}
    </Pressable>
  );
}

export function SettingsTab({ onReset }: { onReset: () => void }) {
  const { state, restoreWeekly, reset } = useStore();

  function onRestore() {
    if (isSubscribed(state.entitlement)) {
      notify("Already active", "Your subscription is active on this account.");
      return;
    }
    restoreWeekly();
    notify("Purchases restored", "Your Catloop subscription is active again.");
  }

  function onDelete() {
    confirmAction(
      "Delete account",
      "This permanently deletes your Catloop account, your videos, and your data on this device. This cannot be undone.",
      "Delete",
      () => {
        reset();
        onReset();
      },
      true
    );
  }

  return (
    <ScrollView contentContainerStyle={styles.scroll} showsVerticalScrollIndicator={false}>
      <View style={styles.section}>
        <View style={styles.planHeader}>
          <Caption style={styles.sectionTitle}>Subscription</Caption>
          <Badge
            label={isSubscribed(state.entitlement) ? "Active" : "Inactive"}
            tone={isSubscribed(state.entitlement) ? "ember" : "gold"}
          />
        </View>
        <View style={styles.card}>
          <Row label="Plan" value={planLabel(state.entitlement)} />
          <View style={styles.sep} />
          <Row label="Video credits" value={`${state.videoCreditsRemaining} left`} />
          <View style={styles.sep} />
          <Row label="Manage subscription" onPress={() => Linking.openURL(LINKS.manageSubscription)} />
          <View style={styles.sep} />
          <Row label="Restore Purchases" onPress={onRestore} />
        </View>
      </View>

      <View style={styles.section}>
        <Caption style={styles.sectionTitle}>AI &amp; safety</Caption>
        <View style={styles.card}>
          <Row
            label="Report AI output"
            onPress={() =>
              notify(
                "Report from a video",
                "Open any video in Create or Library and tap Report to flag unsafe, incorrect, or harmful output."
              )
            }
          />
          <View style={styles.sep} />
          <Row label="How Catloop uses AI" onPress={() => Linking.openURL(LINKS.support)} />
        </View>
        <Caption style={styles.note}>
          Every video is AI-generated by a third-party model from the photo and prompt you provide. Nothing is
          captured by a real camera.
        </Caption>
      </View>

      <View style={styles.section}>
        <Caption style={styles.sectionTitle}>Privacy &amp; legal</Caption>
        <View style={styles.card}>
          <Row label="Privacy Policy" onPress={() => Linking.openURL(LINKS.privacy)} />
          <View style={styles.sep} />
          <Row label="Terms of Use" onPress={() => Linking.openURL(LINKS.terms)} />
          <View style={styles.sep} />
          <Row label="Support" onPress={() => Linking.openURL(LINKS.support)} />
        </View>
      </View>

      <View style={styles.section}>
        <View style={styles.card}>
          <Row label="Delete account" onPress={onDelete} danger />
        </View>
      </View>

      <Text style={styles.footer}>Catloop v1.0 · AI Cat Video Maker</Text>
    </ScrollView>
  );
}

const styles = StyleSheet.create({
  scroll: { padding: spacing(5), paddingBottom: spacing(10), gap: spacing(5) },
  section: { gap: spacing(2) },
  sectionTitle: { textTransform: "uppercase", letterSpacing: 0.5, fontFamily: font.semi, color: colors.inkFaint },
  planHeader: { flexDirection: "row", justifyContent: "space-between", alignItems: "center" },
  card: {
    backgroundColor: colors.surface,
    borderRadius: radius.lg,
    borderWidth: 1,
    borderColor: colors.border,
    paddingHorizontal: spacing(4),
    ...cardShadow,
  },
  row: { flexDirection: "row", justifyContent: "space-between", alignItems: "center", paddingVertical: spacing(4) },
  rowLabel: { fontFamily: font.medium, color: colors.ink, fontSize: 15 },
  rowValue: { fontFamily: font.body, color: colors.inkDim, fontSize: 14 },
  chevron: { color: colors.inkFaint, fontSize: 20 },
  sep: { height: 1, backgroundColor: colors.border },
  footer: { fontFamily: font.body, color: colors.inkFaint, fontSize: 12, textAlign: "center", marginTop: spacing(2) },
  note: {},
});
